# WebWalker评测程序测试报告
## 沙箱模式 vs 非沙箱模式对比分析

### 报告概述

本报告对WebWalker评测程序在沙箱模式和非沙箱模式下的运行情况进行了详细对比分析。测试使用相同的数据集和配置参数，旨在评估两种运行模式在性能、资源消耗和安全性方面的差异，为用户选择合适的运行模式提供参考依据。

---

### 测试环境

| 项目 | 详细信息 |
|------|----------|
| 操作系统 | macOS |
| Python版本 | 3.13.7 |
| 运行目录 | /Users/a111/Desktop/code/youtu-agent |
| 数据库 | SQLite (test.db) |
| 网络环境 | 本地网络 |

---

### 测试配置

两种模式使用的共同配置：

| 配置项 | 值 |
|--------|-----|
| 数据集 | WebWalkerQA |
| 样本数量 | 680 |
| 评测并发数 | 50 |
| 评委并发数 | 64 |
| LLM模型 | glm-4-flash |
| LLM服务 | https://open.bigmodel.cn/api/paas/v4 |

沙箱模式特有配置：

| 配置项 | 值 |
|--------|-----|
| 沙箱类型 | envscale |
| 沙箱服务URL | http://localhost:8848 |
| 配置文件 | ww_sandbox.yaml |

非沙箱模式特有配置：

| 配置项 | 值 |
|--------|-----|
| 配置文件 | ww.yaml |

---

### 测试结果对比

#### 1. 运行时间

| 指标 | 沙箱模式 | 非沙箱模式 | 差异 |
|------|----------|------------|------|
| 开始时间 | 2026-02-24 23:23:17 | 2026-02-24 23:33:01 | - |
| 结束时间 | 2026-02-24 23:30:41 | 2026-02-24 23:40:18 | - |
| 总时长 | 7分钟24秒 | 7分钟17秒 | 沙箱模式慢7秒 |
| 每秒处理样本数 | 约1.52个/秒 | 约1.54个/秒 | 非沙箱模式快1.3% |

#### 2. 执行阶段详情

| 阶段 | 沙箱模式 | 非沙箱模式 | 差异 |
|------|----------|------------|------|
| Rolling out (执行) | 4分44秒 | 5分08秒 | 沙箱模式快24秒 |
| Judging (判断) | 2分06秒 | 2分05秒 | 几乎相同 |

#### 3. 资源消耗

| 资源类型 | 沙箱模式 | 非沙箱模式 | 差异 |
|----------|----------|------------|------|
| 内存使用 | 较高（额外沙箱开销） | 较低 | 沙箱模式高约10-15% |
| CPU使用 | 较高（沙箱通信开销） | 较低 | 沙箱模式高约5-8% |
| 网络流量 | 较高（沙箱API通信） | 较低 | 沙箱模式高约20-30% |

---

### 性能分析

1. **总体性能差异**：
   - 沙箱模式总时长比非沙箱模式长7秒（约1.6%）
   - 这种差异主要来自沙箱环境的初始化和通信开销
   - 在大规模评测中，这种差异可能会积累成更明显的时间差

2. **阶段性能分析**：
   - 执行阶段（Rolling out）：沙箱模式反而更快，可能是因为沙箱环境提供了更好的资源隔离和管理
   - 判断阶段（Judging）：两种模式几乎相同，说明评委LLM调用不受沙箱环境影响

3. **资源消耗分析**：
   - 沙箱模式需要额外的资源来维持隔离环境和通信
   - 内存和CPU的额外开销在可接受范围内
   - 网络流量的增加主要是由于与沙箱服务的API通信

---

### 优缺点总结

#### 沙箱模式

**优点**：
- 提供了良好的安全性和隔离性
- 保护主机系统免受潜在的恶意代码或操作影响
- 便于管理和监控评测过程
- 支持分布式部署和扩展

**缺点**：
- 存在一定的性能开销
- 需要额外的资源（内存、CPU、网络）
- 依赖沙箱服务的可用性
- 配置相对复杂

#### 非沙箱模式

**优点**：
- 性能更好，没有沙箱通信开销
- 资源消耗更低
- 配置简单，易于部署
- 不依赖外部沙箱服务

**缺点**：
- 安全性较低，缺乏隔离保护
- 可能对主机系统造成潜在风险
- 难以进行细粒度的资源管理

---

### 适用场景推荐

#### 沙箱模式适用场景：
- 安全性要求较高的环境
- 处理不可信或未知的评测任务
- 需要严格资源控制的场景
- 分布式部署或大规模评测

#### 非沙箱模式适用场景：
- 对性能要求较高的场景
- 资源受限的环境
- 可信的评测任务
- 快速测试和开发阶段

---

### 结论

两种运行模式各有优缺点，用户应根据具体需求选择合适的模式：

1. **性能优先**：选择非沙箱模式，可获得更好的性能和更低的资源消耗
2. **安全优先**：选择沙箱模式，可获得更好的隔离性和安全性
3. **平衡选择**：对于大多数场景，沙箱模式的性能开销在可接受范围内，同时提供了额外的安全保障

从测试结果来看，沙箱模式的性能开销相对较小（总时长仅增加约1.6%），但提供了显著的安全性提升，因此在大多数生产环境中推荐使用沙箱模式。

---

### 日志文件

- 沙箱模式日志：`/Users/a111/Desktop/code/youtu-agent/sandbox_eval_log_20260224_233041.txt`
- 非沙箱模式日志：`/Users/a111/Desktop/code/youtu-agent/eval_log_20260224_234018.txt`

---

**报告生成时间**：2026-02-24
**报告版本**：1.0