# 评测脚本具体评测内容介绍

评测脚本是一个完整的自动化评测框架，用于评估智能体的性能。以下是对评测内容和流程的详细介绍：

## 1. 评测框架结构

评测脚本的核心是 `BaseBenchmark` 类，它定义了完整的评测流程。主要组成部分包括：

- **评测配置**：通过 `EvalConfig` 定义评测参数
- **数据管理**：通过 `DBDataManager` 管理评测数据
- **评测处理器**：通过不同的 `Processer` 处理不同类型的评测任务
- **评测流程**：包含预处理、执行、判断和统计四个主要阶段

## 2. 评测流程

评测脚本遵循以下四个主要阶段：

### 2.1 预处理阶段（preprocess）
- 加载初始状态的评测样本
- 对每个样本进行预处理（如生成增强问题）
- 更新样本状态为预处理完成

### 2.2 执行阶段（rollout）
- 加载预处理完成的评测样本
- 并发执行智能体，获取智能体对每个问题的回答
- 记录智能体的响应、执行时间和轨迹信息
- 更新样本状态为执行完成

### 2.3 判断阶段（judge）
- 加载执行完成的评测样本
- 使用LLM作为评委，判断智能体回答的正确性
- 记录判断结果、推理过程和置信度
- 更新样本状态为判断完成

### 2.4 统计阶段（stat）
- 加载判断完成的评测样本
- 计算各种评测指标
- 生成评测报告

## 3. 评测处理器类型

评测脚本支持多种评测处理器，用于处理不同类型的评测任务：

- **WebWalkerQAProcesser**：处理WebWalkerQA类型的评测任务
- **BrowseCompProcesser**：处理BrowseComp类型的评测任务
- **GAIAProcesser**：处理GAIA类型的评测任务
- **XBenchProcesser**：处理XBench类型的评测任务
- **TrainingFreeGRPOProcesser**：处理训练免费的GRPO评测任务

## 4. 评测判断逻辑

评测使用LLM作为评委，基于以下逻辑判断回答的正确性：

### 4.1 精确匹配检查
首先检查智能体的回答是否与标准答案完全匹配，如果是则直接判定为正确。

### 4.2 LLM评委判断
如果没有精确匹配，则使用LLM评委进行判断：

1. 根据评测类型选择对应的判断提示模板
2. 将问题、智能体回答和标准答案输入给LLM评委
3. LLM评委生成判断结果和推理过程
4. 解析LLM评委的输出，提取判断结果、推理过程和置信度

## 5. 评测指标

评测脚本计算以下主要指标：

- **Pass@k**：在k次尝试中至少有一次正确的比例
- **Level-based Pass@k**：按难度级别统计的Pass@k指标

## 6. 评测配置

评测脚本通过配置文件定义评测参数，包括：

- 实验ID（exp_id）
- 智能体模型配置
- 数据集配置
- 并发执行数
- 评委并发数
- Pass@k的k值

## 7. 评测执行方式

评测脚本可以通过命令行执行，支持多种执行模式：

```bash
# 执行完整评测流程
python scripts/run_eval.py --config_name ww

# 仅执行预处理和执行阶段
python scripts/run_eval.py --config_name ww --step rollout

# 仅执行判断和统计阶段
python scripts/run_eval.py --config_name ww --step judge
```

## 8. 评测结果存储

评测结果存储在数据库中，包括：

- 原始问题和增强问题
- 智能体的响应和执行时间
- 智能体的轨迹信息
- LLM评委的判断结果和推理过程
- 评测指标

这些结果可以通过前端界面进行查看和分析，帮助用户了解智能体的性能表现。